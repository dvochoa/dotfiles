interface:
  display_name: "Ship"
  short_description: "Commit, push, and update the pull request"
  default_prompt: "Use $ship to commit, push, and update the current pull request."
policy:
  allow_implicit_invocation: false
