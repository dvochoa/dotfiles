---
name: ship
description: Commit the current changes, push the branch, and create or update its pull request. Use when explicitly asked to ship a branch or invoke $ship.
---

Read `~/.agents/workflows/ship.md` and follow it exactly.
