---
name: iterate
description: Address review feedback on the current pull request, then ship the changes. Use when explicitly asked to iterate on a pull request or invoke $iterate.
---

Read `~/.agents/workflows/iterate.md` and follow it exactly.
